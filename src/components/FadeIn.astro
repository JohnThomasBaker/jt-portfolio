---
const { delay = 0, y = 12, once = true, blur = 0, duration, bg } = Astro.props;
---

<div
  class="fadein"
  data-delay={delay}
  data-y={y}
  data-blur={blur}
  data-once={once ? "1" : "0"}
  data-dur={duration}
  data-bg={bg}
>
  <slot />
</div>